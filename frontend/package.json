{
  "name": "maths-instruments-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run",
    "watch": "vitest"
  },
  "dependencies": {
    "katex": "^0.16.10"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "vitest": "^1.6.0",
    "typescript": "^5.4.0",
    "@types/katex": "^0.16.7"
  }
}
